# Python
*.pyc
__pycache__/
*.egg-info/
.venv/
venv/

# Vercel
.vercel

# Secrets & local config (NEVER commit these — safety net for future)
.env
.env.local
.env.*.local

# Local caches & backups (e.g. translate.py rewrites daily_data.json → .cache/*.bak)
.cache/
*.json.bak
daily_data_backup_*.json

# OS
.DS_Store
Thumbs.db

# Node — four-persimmons Next app & root install cache
/node_modules/
sites/four-persimmons/node_modules/
sites/four-persimmons/.next/
sites/four-persimmons/out/
# Local image download config (may contain signed / internal URLs)
sites/four-persimmons/bakery-asset-urls.json
